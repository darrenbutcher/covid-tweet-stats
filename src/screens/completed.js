import React from "react"
import FullScreen from "../molecules/full-screen"

const Completed = () => (
  <FullScreen center>
    <h2 style={{ paddingBottom: "16px", textAlign: "center" }}>Yay!</h2>
    <p style={{ textAlign: "center" }}>
      You have completed the creating your profile.
    </p>
  </FullScreen>
)

export default Completed
